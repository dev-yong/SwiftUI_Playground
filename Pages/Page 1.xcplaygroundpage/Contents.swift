//: [Previous](@previous)

import Combine
import SwiftUI
import PlaygroundSupport

let view = VStack {
    
    Text("Hello, world")
}
PlaygroundPage.current.liveView = UIHostingController(rootView: view)

//: [Next](@next)
